<?xml version="1.0" encoding="UTF-8"?>
<!-- Schema 추가 -->
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.acadmi.notification.NotificationDAO">
  	<sql id="notificationKind">
  		<where>
  			<if test=""></if>
  		</where>
  	</sql>
  	<!-- notification list -->
  	<select id="getList" parameterType="NotificationVO" resultType="NotificationVO">
  		SELECT * FROM NOTIFICATION
  		WHERE NOTIFICATIONKIND = #{notificationKind}
  	</select>
    
  </mapper>